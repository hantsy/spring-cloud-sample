spring:
  application:
    name: gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    resource:
      path: /posts/**
      url: http://localhost:8002/
    user:
      path: /**
      url: http://localhost:8001/
